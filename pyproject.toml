[project]
name = "pefftacular"
version = "0.1.0"
description = "A pure-Python library for reading and writing PEFF (PSI Extended FASTA Format) files."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []
authors = [
    { name = "Patrick Garrett", email = "pgarrett@scripps.edu" }
]
license = {text = "MIT"}
keywords = ["peff", "proteomics", "fasta", "bioinformatics", "mass-spectrometry"]
maintainers = [
    { name = "Patrick Garrett", email = "pgarrett@scripps.edu" }
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Bio-Informatics",
    "Typing :: Typed",
]

[project.urls]
Repository = "https://github.com/pgarrett-scripps/pefftacular"
Issues = "https://github.com/pgarrett-scripps/pefftacular/issues"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/pefftacular"]

[tool.uv]
package = true
dev-dependencies = [
    "pytest>=9.0.2",
    "pytest-cov>=6.0",
    "ruff>=0.14.11",
    "ty>=0.0.11",
]

[tool.ruff]
target-version = "py312"
line-length = 120

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # Pyflakes
    "I",   # isort
    "B",   # flake8-bugbear
    "UP",  # pyupgrade
]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.coverage.run]
source = ["src/pefftacular"]
omit = ["*/__pycache__/*"]

[tool.coverage.report]
show_missing = true
skip_covered = false
